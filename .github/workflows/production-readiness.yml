name: Production readiness

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate production files and safeguards
        shell: pwsh
        run: ./scripts/verify-production.ps1
